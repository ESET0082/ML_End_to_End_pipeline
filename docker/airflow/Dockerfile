FROM apache/airflow:2.8.0

# Install dependencies with compatible versions
RUN pip install --no-cache-dir \
    numpy>=1.24.0 \
    pandas \
    scikit-learn \
    joblib \
    psycopg2-binary \
    mlflow
